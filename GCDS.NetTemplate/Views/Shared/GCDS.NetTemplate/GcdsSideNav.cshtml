@using GCDS.NetTemplate.Components

@model GcdsSideNav

<gcds-side-nav label="@Model.Label"
			  style="@Model.StyleOverride"
              class="@(Model.Scroll ? "sidenav-scroll lg:position-sticky align-self-start border-box" : "")">

    @await Html.PartialAsync("SlotNavLinks.cshtml", Model.Links)

</gcds-side-nav>
