@using GCDS.NetTemplate.Components

@model ExtAppHeaderTop

<gcds-container layout="page"
				alignment="centered"				
				style="padding: var(--gcds-header-brand-padding); @Model.StyleOverride">

	@{
		Model.LanguageToggle.StyleOverride += "text-align:end";
	}
	@await Html.PartialAsync("GcdsLangToggle.cshtml", Model.LanguageToggle)

	@await Html.PartialAsync("ExtSiteTitle.cshtml", Model.SiteTitle)

</gcds-container>
